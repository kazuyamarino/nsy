<?php
/**
 * View template (MVC). Rendered by Razr via Load::view(null, 'cm_view', $data).
 * Use @( $var ) for escaped output and @raw($var) for unescaped.
 */
?>
<div class="wrap">
	<h2>@( $title )</h2>
	<p>Hello from <code>cm_view</code>.</p>
</div>
