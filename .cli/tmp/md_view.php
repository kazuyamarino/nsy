<?php
/**
 * View template (HMVC module). Rendered by Razr via Load::view('ModuleName', 'md_view', $data).
 * Use @( $var ) for escaped output and @raw($var) for unescaped.
 */
?>
<div class="wrap">
	<h2>@( $title )</h2>
	<p>Hello from <code>md_view</code> (module <code>ModuleName</code>).</p>
</div>
